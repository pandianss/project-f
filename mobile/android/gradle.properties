org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.keepDebugSymbols=true
# NOTE: org.gradle.java.home is intentionally NOT set here (it would hardcode a
# machine-specific path and break clones/CI). If Gradle can't find a JDK, set it
# in your user-global ~/.gradle/gradle.properties or via the JAVA_HOME env var.
